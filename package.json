{
  "name": "campusconnect-root",
  "version": "1.0.0",
  "description": "Root configuration for CampusConnect fullstack deployment",
  "scripts": {
    "build:frontend": "npm install --include=dev --prefix frontend && npm run build --prefix frontend",
    "build:backend": "npm install --prefix backend",
    "build": "npm run build:frontend && npm run build:backend",
    "start": "node backend/server.js"
  },
  "dependencies": {
    "axios": "^1.14.0",
    "react-router-dom": "^7.13.2"
  }
}
